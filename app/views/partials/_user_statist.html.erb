<div class="stats">
  <table>
    <tbody>
      <tr>
        <th>Tweets</th>
        <th>Followings</th>
        <th>Followers</th>
      </tr>
      <tr>
        <td><%= link_to user.tweets.count, my_tweets_user_path(user) %></td>
        <td><%= link_to user.followings.count,  followings_user_path(user) %></td>
        <td><%=link_to user.followers.count, followers_user_path(user) %></td>
      </tr>
    </tbody>
  </table>
</div>
